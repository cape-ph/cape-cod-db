# For sqlite (not thoroughly tested) this would look something like
# DB_URL="sqlite:///cape_env_db.db"
DB_URL="postgresql:///cape_env_db"

# uppercase string of the python logging constant (e.g. "DEBUG", "WARNING",
# "INFO", etc)
LOG_LEVEL="DEBUG"
